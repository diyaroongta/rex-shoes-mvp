/** Classes in App.jsx were authored against Tailwind v3. Pinned deliberately —
 *  v4 renames some utilities (shadow-sm, outline-none) and would shift the look. */
export default {
  content: ["./index.html", "./src/**/*.{js,jsx}"],
  theme: { extend: {} },
  plugins: [],
};
